################### 추가 문법2 ######################



# 비트 연산자:  ~(bitwise not), &(bitwise and), |(bitwise or), ^(bitwise xor)

''' 
    줄바꿈으로 임의의 여러값 받기
    import sys
    data = sys.stdin.read().strip()
    list = list(map(int,data.split()))
'''

